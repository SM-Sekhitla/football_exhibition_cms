import type { ReactNode } from 'react';

export function AdminPageHeader({ title, eyebrow = 'BEST5 CMS', action }: { title: string; eyebrow?: string; action?: ReactNode }) { return <div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[.3em] text-[#c8ff4b]">{eyebrow}</p><h1 className="display mt-2 text-5xl font-black uppercase">{title}</h1></div>{action}</div>; }
export function StatusBadge({ value }: { value: string }) { const good = ['Published','Approved','Live'].includes(value); const bad = ['Rejected','Cancelled'].includes(value); return <span className={`rounded-full px-3 py-1 text-[10px] font-black uppercase ${good ? 'bg-[#c8ff4b]/15 text-[#c8ff4b]' : bad ? 'bg-red-400/15 text-red-300' : 'bg-amber-300/15 text-amber-200'}`}>{value}</span>; }
export function EmptyState({ text }: { text: string }) { return <div className="rounded-2xl border border-dashed border-white/15 p-10 text-center text-sm text-white/45">{text}</div>; }
export function FormField({ label, children }: { label: string; children: ReactNode }) { return <label className="block"><span className="mb-2 block text-[10px] font-bold uppercase tracking-widest text-white/50">{label}</span>{children}</label>; }
export function StatCard({ label, value }: { label: string; value: string | number }) { return <div className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><span className="text-[10px] font-bold uppercase tracking-widest text-white/40">{label}</span><b className="display mt-3 block text-4xl text-[#c8ff4b]">{value}</b></div>; }
