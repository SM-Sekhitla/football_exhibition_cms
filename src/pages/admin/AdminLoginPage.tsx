import { useState, type FormEvent } from 'react';
import { Eye, EyeOff, LoaderCircle } from 'lucide-react';
import { authService } from '@/services/authService';
import { settingsService } from '@/services/cmsServices';

export function AdminLoginPage({ navigate }: { navigate: (path: string) => void }) { const [identity, setIdentity] = useState(''); const [password, setPassword] = useState(''); const [show, setShow] = useState(false); const [remember, setRemember] = useState(true); const [loading, setLoading] = useState(false); const [error, setError] = useState(''); const submit = async (e: FormEvent) => { e.preventDefault(); setLoading(true); setError(''); const ok = await authService.login(identity, password, remember); setLoading(false); if (ok) navigate('/admin'); else setError('Incorrect email or password.'); }; return <main className="flex min-h-screen items-center justify-center bg-[#07100c] p-5 text-white"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-white/10 bg-[#0d1d14] p-8 shadow-2xl"><img src={settingsService.get().branding.mainLogo} alt="BEST5" className="h-16 w-16 rounded-xl object-cover" /><p className="mt-8 text-xs font-black uppercase tracking-[.3em] text-[#d8b852]">Secure administration</p><h1 className="display mt-2 text-5xl font-black uppercase">Admin Portal</h1><div className="mt-8 space-y-5"><label className="block"><span className="mb-2 block text-xs text-white/55">Email / Username</span><input autoFocus required value={identity} onChange={e => setIdentity(e.target.value)} className="field" autoComplete="username" /></label><label className="block"><span className="mb-2 block text-xs text-white/55">Password</span><div className="relative"><input required type={show ? 'text' : 'password'} value={password} onChange={e => setPassword(e.target.value)} className="field pr-12" autoComplete="current-password" /><button type="button" aria-label={show ? "Hide password" : "Show password"} onClick={() => setShow(!show)} className="absolute right-1 top-1/2 flex h-11 w-11 -translate-y-1/2 items-center justify-center text-white/65">{show ? <EyeOff size={18} /> : <Eye size={18} />}</button></div></label><label className="flex items-center gap-3 text-xs text-white/60"><input type="checkbox" checked={remember} onChange={e => setRemember(e.target.checked)} className="accent-[#c8ff4b]" /> Remember me</label>{error && <p role="alert" className="rounded-xl bg-red-400/10 p-3 text-sm text-red-300">{error}</p>}<button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-full bg-[#c8ff4b] py-4 text-xs font-black uppercase tracking-widest text-[#07100c] disabled:opacity-60">{loading && <LoaderCircle className="animate-spin" size={16} />}{loading ? 'Signing in…' : 'Login'}</button></div></form></main>; }
